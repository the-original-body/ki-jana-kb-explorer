/**
 * Client Entry Point
 *
 * This file is the entry point for client-side hydration in the browser.
 * It initializes the React application and enables client-side interactivity.
 *
 * Note: CSS is imported in AppDocument.tsx using ?url suffix for SSR compatibility.
 */

import "rwsdk/client";
